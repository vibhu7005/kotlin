package kt.arrays;

import java.util.ArrayList;
import java.util.Arrays;

public class StudentNames {
    void printNames (int array[]) {
        for (int i= 0; i<array.length; i++) {
            System.out.println(array[i]);
        }
    }
}
