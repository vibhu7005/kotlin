package kt.dataTypes;

public class Bird {
    public void canFly(boolean canFly) {
        if (canFly)
            System.out.println("yes bird can fly");
        else
            System.out.println("No bird can't fly");
    }
}
